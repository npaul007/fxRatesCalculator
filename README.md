# fxRatesCalculator
Currency Converter written in php.

# Features
-Accesses a mySQL database to obtain the conversion rates and login information.<br />
-Uses Session variables to handle cookie management.<br />
-Gives user the option of sending the currency conversion via email through an SMTP server.

